export const INTERN_STATUSES = Object.freeze(['Active', 'Onboarding', 'Completed']);

export const INTERN_DEPARTMENTS = Object.freeze(['Engineering', 'Design', 'Marketing']);

export const DEFAULT_PAGE_LIMIT = 10;
export const MAX_PAGE_LIMIT = 50;
